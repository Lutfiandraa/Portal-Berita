# PostgreSQL
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=your_password
PGDATABASE=portal_berita
PGPORT=5432

# Optional: CORS (comma-separated). Default: http://localhost:3000,http://localhost:3001
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Optional: Session secret
# SESSION_SECRET=your-secret-key
